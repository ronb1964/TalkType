# ============================================
# Python
# ============================================
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
.installed.cfg
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
MANIFEST

# ============================================
# Virtual environments
# ============================================
.env
.venv
env/
venv/
ENV/

# ============================================
# Testing & Coverage
# ============================================
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# ============================================
# IDE / Editor
# ============================================
.idea/
.vscode/
*.swp
*.swo
*~

# ============================================
# AppImage build artifacts
# ============================================
*.AppImage
*.AppImage.zsync
SHA256SUMS.txt
# .deb / .rpm packages built by build-deb.sh / build-rpm.sh
# (attached to GitHub releases, not tracked)
*.deb
*.rpm
appimage-builder
appimagetool
appimagetool-extracted/
AppDir/
squashfs-root/

# ============================================
# Build logs
# ============================================
*.log

# ============================================
# CUDA / GPU (too large for GitHub)
# ============================================
cuda_wheels/

# ============================================
# Internal dev notes & planning docs
# (kept locally, not needed on public repo)
# ============================================
APPIMAGEHUB_STATUS.md
APPIMAGEHUB_SUBMISSION.md
CLAUDE.md
CROSS_PLATFORM_PORTING_GUIDE.md
DEVELOPMENT_PLAN.md
DEVELOPMENT_SEPARATION.md
DEV_NOTES.md
GNOME_EXTENSION_FIXES_NEEDED.md
GNOME_EXTENSION_IDEAS.md
ICON_DOCUMENTATION.md
IMAGE_GENERATION_TOOLS.md
MARKETING_PLAN.md
MCP_SCREENSHOT_USAGE.md
MCP_SERVER_DEBUG_SUMMARY.md
NO_PREINSTALL_BUNDLING_NOTES.md
REPOSITORY_STRUCTURE.md
ROADMAP_v*.md
STABLE_DIFFUSION_SETUP.md
VISUAL_STYLE_GUIDE.md
marketing-posts.md

# ============================================
# Dev/test scripts (root level)
# (proper unit tests in tests/ are tracked)
#
# NOTE: the build and release toolchain does NOT belong in this section.
# container-build.sh, patch_pytorch.py, package-extension.sh and
# fresh-start-for-testing.sh were once filed here by mistake, which left a
# clone of this repository unable to build or release itself — build-release.sh
# is tracked but calls container-build.sh, which was not. Keep anything the
# documented release process in CLAUDE.md depends on OUT of this list.
# ============================================
test-*.sh
test-*.py
test_*.py
!tests/test_*.py
list-atspi-apps.py
mcp-screenshot-server.py
launch-gtk-inspector.sh
install-xdotool.sh
regenerate-autostart*.py
cleanup-docker-artifacts.sh
build-with-appimage-builder.sh
fresh-start*.sh
!fresh-start-for-testing.sh
fresh-test-env.sh

# ============================================
# AUR publishing clone
# ============================================
# aur-repo/ is a working clone of the real AUR package repository
# (ssh://aur@aur.archlinux.org/talktype-appimage.git) used to push releases to
# Arch. It has its own .git, so committing it into this repository recorded it
# as a submodule with no .gitmodules — which made `git clone
# --recurse-submodules` fail and showed as a dead link on GitHub. The packaging
# sources that belong to this project live in aur/ and are tracked there.
aur-repo/

# ============================================
# Build artifacts & generated files
# ============================================
talktype-gnome-extension.zip
test-screenshots/
icons/
*.svg.playwright-mcp/

# ============================================
# Archive / old files
# ============================================
archive/

# ============================================
# Claude Code
# ============================================
.claude/

# Playwright MCP automation logs
.playwright-mcp/

# Flatpak build artifacts
.flatpak-build/
.flatpak-builder/
.flatpak-repo/
